const { JSDOM } = require('jsdom'); const dom = new JSDOM('<div id="root"></div>', { url: 'https://ai-erp-ite.github.io/Watch-Faces/admin', runScripts: 'dangerously', pretendToBeVisual: true }); const fs = require('fs'); const script = fs.readFileSync('dist/assets/index-DFmkqk9P.js', 'utf8'); try { dom.window.eval(script); } catch (e) { console.error('EVAL ERROR:', e); } setTimeout(() => { console.log('INNER:', dom.window.document.body.innerHTML.substring(0, 200)); }, 2000);

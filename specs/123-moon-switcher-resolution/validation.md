# Validation

- [ ] TypeScript and focused tests pass.
- [ ] `npm run build:private` succeeds after Firebase environment preflight.
- [ ] Compiled bundle contains `Moon Resolution` and `LUNAR_CYCLE` behavior.
- [ ] Existing unrelated working-tree changes remain untouched.
- [ ] Spec and implementation are committed separately.
- [ ] `npm run deploy:full:private` pushes only to `origin/main`.
- [ ] Root, Studio, and Parametric live entrypoints reference the same production hash.
- [ ] SPA redirect query route resolves and deployed JS/CSS assets return HTTP 200.

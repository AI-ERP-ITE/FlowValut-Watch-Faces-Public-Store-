# Plan

1. Extend the existing switcher policy/count helpers with `LUNAR_CYCLE` and the allowed Moon resolutions.
2. Parameterize the existing Moon slot builder and add Moon-specific validation.
3. Add the Moon-only resolution control to the existing Switcher Lab and reuse fixed-slot presentation.
4. Apply a linked definition's count in the existing Property Panel link handler.
5. Add focused policy/resolver/generator regression tests, then run private build and verification.
6. Deploy with the canonical private full-deploy script and verify live bundle/route parity.


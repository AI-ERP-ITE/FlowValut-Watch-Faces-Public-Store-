# Task Tracker — Spec 121

- [x] T1 — Trace composer, IndexedDB bake, Interactive Canvas, and V2/V3 export hub dimensions; confirm canvas and watch already agree and isolate the master-to-device mismatch.
- [x] T2 — Add PNG-only adaptive hub normalization while preserving HTML/SVG hubs, source masters, pointer pivots, and center logic.
- [x] T3 — Add PNG-only rebake migration for existing source-backed records without changing hand assets or positioning.
- [x] T4 — Run TypeScript, repository verification, private build/deploy, and live bundle parity checks; close the spec with deployment evidence. (52/52 verifier checks; deployment `17c51703`, bundle `index-SUaR0KS2.js`.)

## Closure

**CLOSED — 2026-07-16.** All four tasks are complete. Golden 01/02 source positioning and every time-pointer pivot/center calculation were deliberately left unchanged.

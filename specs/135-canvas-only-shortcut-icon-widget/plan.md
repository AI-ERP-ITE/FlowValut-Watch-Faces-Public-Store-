# Spec 135 Implementation Plan

1. Add the widget identity and one shared canvas-only boundary helper.
2. Render the existing premium shortcut marker directly on MAIN canvas.
3. Apply the exclusion helper to AOD sync, AOD presentation, and export.
4. Remove the old static-library entry and verify with tests/build inspection.


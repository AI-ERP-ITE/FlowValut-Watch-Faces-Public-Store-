# Plan

1. Record byte-parity hashes for copied foundation files.
2. Add the literal System A pointer preparation module.
3. Prepare all FVWC source configurations before editable compilation.
4. Replace generated edit masks with the default variant preview.
5. Remove the optional second-stage selector configuration.
6. Add regression and archive tests.
7. Build, deploy privately, and verify live hashes.

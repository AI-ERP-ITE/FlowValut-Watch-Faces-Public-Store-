# Implementation Plan

1. Centralize read-model device compatibility and price selection.
2. Correct homepage fallbacks and implement the zero-compatible empty state.
3. Move search and discovery controls to the read model; render public download totals.
4. Add durable per-order/per-package usage with transactional reservations.
5. Restrict regeneration to one token with one final successful transfer.
6. Aggregate completed transfers and unique orders; add authenticated admin reporting.
7. Add self-service recovery UI and align legal policies.
8. Build, test, scan, deploy targeted Functions, deploy staging storefront, and verify.
